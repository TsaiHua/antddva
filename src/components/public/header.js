import React from 'react';
import styles from '../layouts.less';

// 方法
const Header = (props) => {
  return (
    <div>
      
    </div>
  );
};

// 参数验证
Header.propTypes = {
};

// 暴露方法
export default Header;
